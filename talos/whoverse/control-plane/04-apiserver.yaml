cluster:
  apiServer:
    extraArgs:
      # Randomly send GOAWAY on ~0.1% of HTTP/2 requests so long-lived clients
      # (kubelets, controllers via KubePrism) periodically reconnect and rebalance
      # across the three apiservers instead of piling onto whichever was up first.
      goaway-chance: "0.001"
