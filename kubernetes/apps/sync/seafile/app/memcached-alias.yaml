---
# Alias service for memcached - Seafile Pro hardcodes 'memcached' as the hostname
apiVersion: v1
kind: Service
metadata:
  name: memcached
  namespace: sync
spec:
  type: ExternalName
  externalName: seafile-memcached.sync.svc.cluster.local
