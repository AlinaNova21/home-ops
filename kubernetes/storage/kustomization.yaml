---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ns.yaml
  # - openebs-localpv/ks.yaml  # disabled pending cluster reset
  # - rook-ceph/ks.yaml  # disabled — no workloads use ceph-rbd; mon PVCs on miroir-replicated
  - miroir/ks.yaml
  - zot/ks.yaml
