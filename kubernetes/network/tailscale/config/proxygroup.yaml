---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/tailscale.com/proxygroup_v1alpha1.json
apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: ingress-proxies
  namespace: network
spec:
  type: ingress
  replicas: 3
  proxyClass: low-resources
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/tailscale.com/proxygroup_v1alpha1.json
apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: pve-egress
  namespace: network
spec:
  type: egress
  proxyClass: low-resources
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/tailscale.com/proxygroup_v1alpha1.json
apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: moltbot-egress
  namespace: network
spec:
  type: egress
  proxyClass: low-resources
