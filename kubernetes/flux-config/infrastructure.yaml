apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system
spec:
  interval: 10m
  path: "./infrastructure"
  sourceRef:
    kind: OCIRepository
    name: home-ops
  timeout: 15m
  wait: true
  prune: true