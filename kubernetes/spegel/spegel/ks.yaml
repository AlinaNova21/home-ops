apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: spegel
  namespace: spegel
spec:
  interval: 15m
  path: "./spegel/spegel/app"
  sourceRef:
    kind: OCIRepository
    name: home-ops
    namespace: flux-system
  wait: true
  prune: false
