---
apiVersion: cdi.kubevirt.io/v1beta1
kind: CDI
metadata:
  name: cdi
  namespace: cdi
spec:
  config:
    scratchSpaceStorageClass: miroir-local
    podResourceRequirements:
      requests:
        cpu: 100m
        memory: 60Mi
      limits:
        cpu: 750m
        memory: 2Gi
